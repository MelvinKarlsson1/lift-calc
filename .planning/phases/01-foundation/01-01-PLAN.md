---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/index.css
  - index.html
  - tsconfig.json
  - tsconfig.app.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npm run dev` starts a Vite dev server without errors"
    - "The browser renders a page (any content) at localhost:5173"
    - "TypeScript compiles without errors (`npm run build` exits 0)"
    - "Tailwind utility classes produce styles (bg-gray-950 produces a dark background)"
    - "The root #root element uses min-h-dvh (not 100vh) in index.css"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with pinned dependencies"
      contains: "\"vite\": \"7.3.1\""
    - path: "vite.config.ts"
      provides: "Build config with React + Tailwind v4 plugins"
      contains: "tailwindcss()"
    - path: "src/index.css"
      provides: "Global CSS with Tailwind import and dvh viewport fix"
      contains: "min-height: 100dvh"
    - path: "src/main.tsx"
      provides: "React entry point"
      contains: "createRoot"
  key_links:
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "@tailwindcss/vite plugin"
      pattern: "tailwindcss\\(\\)"
    - from: "src/index.css"
      to: "browser"
      via: "Tailwind @import directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Scaffold the lift-calc project with the locked tech stack and apply the iOS viewport fix from day one.

Purpose: Every subsequent plan builds on this shell. The Vite + React + TypeScript + Tailwind v4 config must be correct before any feature code is written, and the dvh fix must be in place before any layout work begins.
Output: A running dev server, a TypeScript-clean build, and a root CSS that uses `100dvh` (not `100vh`) everywhere the shell needs to fill the screen.
</objective>

<execution_context>
@C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- No existing interfaces — greenfield scaffold. -->
<!-- Key constraint: Vite is pinned to 7.3.1. Do NOT use 8.x. -->
<!-- vite-plugin-pwa is NOT installed in this phase. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project, install dependencies, and apply dvh viewport fix</name>
  <files>
    package.json
    vite.config.ts
    src/index.css
    src/main.tsx
    src/App.tsx
    index.html
    tsconfig.json
    tsconfig.app.json
    .gitignore
  </files>
  <read_first>
    - C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/lift-calc/.planning/phases/01-foundation/01-RESEARCH.md (full file — contains exact install commands, vite.config.ts template, and index.css template)
  </read_first>
  <action>
The project directory `lift-calc/` already exists but has no `package.json`. Run the scaffold from the PARENT directory `MK-Claude-Learning-Tool/`, targeting the existing `lift-calc/` subfolder name.

IMPORTANT: Run all commands from `C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/` (the parent), not from inside `lift-calc/`.

Step 1 — Scaffold (run from parent directory):
```bash
cd "C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool"
npm create vite@latest lift-calc -- --template react-ts
```
When prompted to confirm overwriting the existing `lift-calc/` directory (it has CLAUDE.md in it), answer YES / proceed. The scaffold will NOT overwrite `.planning/` subdirectories.

Step 2 — Install core dependencies (run from lift-calc/):
```bash
cd "C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/lift-calc"
npm install
npm install vite@7.3.1
npm install tailwindcss @tailwindcss/vite
npm install zustand
```

Step 3 — Replace `vite.config.ts` with the exact content below (replaces the Vite default):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    // vite-plugin-pwa added in a later phase
  ],
})
```

Step 4 — Replace `src/index.css` with the exact content below (replaces Vite default CSS):
```css
@import "tailwindcss";

/* iOS Safari viewport fix — use dvh everywhere, never raw 100vh */
html {
  height: -webkit-fill-available; /* iOS Safari 14 and earlier fallback */
}

body {
  min-height: 100vh;
  min-height: 100dvh; /* overrides line above in supporting browsers */
}

#root {
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
}
```

Step 5 — Replace `src/App.tsx` with a minimal placeholder (store wiring comes in Plan 02):
```tsx
export default function App() {
  return (
    <div className="min-h-[100dvh] flex flex-col bg-gray-950 text-white">
      <main className="flex-1 p-4">
        <p className="text-gray-500 text-sm">Lift Calc — foundation ready.</p>
      </main>
    </div>
  )
}
```

Step 6 — Delete the Vite boilerplate files that are not needed:
- Delete `src/assets/react.svg` (if it exists)
- Delete `public/vite.svg` (if it exists)
- Delete `src/App.css` (if it exists)

Step 7 — Verify the dev server runs:
```bash
npm run dev
```
The terminal should print a localhost URL (default: http://localhost:5173). Press Ctrl+C to stop.

Step 8 — Verify the production build is TypeScript-clean:
```bash
npm run build
```
Must exit 0. If TypeScript errors appear, fix them before proceeding.

NOTE: Do NOT install `vite-plugin-pwa` in this phase. It is deferred to a later phase to avoid service worker complexity during development.
  </action>
  <verify>
    <automated>cd "C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/lift-calc" && npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no TypeScript errors
    - `package.json` contains `"vite": "7.3.1"` (exact version, not `^7`)
    - `vite.config.ts` contains `tailwindcss()` in the plugins array
    - `src/index.css` contains `@import "tailwindcss"` on line 1
    - `src/index.css` contains `min-height: 100dvh` (not `min-height: 100vh` only)
    - `src/index.css` contains `-webkit-fill-available` on the `html` rule
    - `src/App.tsx` uses `min-h-[100dvh]` (not `h-screen` or `min-h-screen`)
    - No `src/App.css` file exists (deleted)
  </acceptance_criteria>
  <done>Vite dev server starts, production build exits 0, Tailwind v4 is active, dvh viewport fix is in place, and no Vite boilerplate remains.</done>
</task>

</tasks>

<verification>
Run from `lift-calc/` directory:
1. `npm run build` — exits 0, no errors
2. `grep -r "min-height: 100dvh" src/index.css` — finds the dvh rule
3. `grep "vite" package.json` — shows `"vite": "7.3.1"` pinned
4. `grep "tailwindcss" vite.config.ts` — confirms Tailwind v4 plugin registered
</verification>

<success_criteria>
- Vite dev server runs (`npm run dev` prints localhost URL)
- Production build succeeds (`npm run build` exits 0)
- Tailwind v4 Vite plugin wired (no PostCSS config needed)
- iOS dvh fix in place before any layout work begins
- Vite pinned to 7.3.1 in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at the execution_context path above.
</output>
