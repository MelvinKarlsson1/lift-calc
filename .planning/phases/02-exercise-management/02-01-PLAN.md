---
phase: 02-exercise-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/types.ts
  - src/store/store.ts
  - src/data/seedExercises.ts
autonomous: true
requirements: [EXER-01, EXER-02]

must_haves:
  truths:
    - "On first launch the exercises array is populated with ~50 exercises (not empty)"
    - "Removing an exercise from the store filters it out of the exercises array"
    - "Removed exercises stay removed after a simulated page reload (localStorage persists the filtered array)"
    - "The seed data never reloads when exercises becomes empty at runtime (only at hydration when empty)"
  artifacts:
    - path: "src/data/seedExercises.ts"
      provides: "Static array of ~50 Exercise objects with kebab-case IDs and isCustom: false"
      exports: ["SEED_EXERCISES"]
    - path: "src/store/types.ts"
      provides: "AppStore interface with removeExercise action"
      contains: "removeExercise: (id: string) => void"
    - path: "src/store/store.ts"
      provides: "removeExercise action + seed loading in persist merge callback"
      contains: "removeExercise"
  key_links:
    - from: "src/store/store.ts"
      to: "src/data/seedExercises.ts"
      via: "import SEED_EXERCISES"
      pattern: "import.*SEED_EXERCISES.*from"
    - from: "src/store/store.ts persist merge"
      to: "SEED_EXERCISES"
      via: "merged.exercises.length === 0 guard"
      pattern: "exercises\\.length === 0"
---

<objective>
Add the `removeExercise` store action and wire in seed data loading so the app populates its exercise list on first launch and persists removals reliably.

Purpose: The UI in Plan 02 depends on both the remove action and a pre-populated exercise list — this plan establishes those contracts first.
Output: `src/data/seedExercises.ts` (50 exercises), updated `src/store/types.ts` (AppStore with removeExercise), updated `src/store/store.ts` (removeExercise action + seed loading in merge callback).
</objective>

<execution_context>
@C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-exercise-management/02-RESEARCH.md

<interfaces>
<!-- Current types.ts contracts (read the actual file before editing) -->
From src/store/types.ts:
```typescript
export interface Exercise {
  id: string
  name: string
  isCustom: boolean
}

export interface AppState {
  schemaVersion: number
  exercises: Exercise[]
  maxWeights: Record<string, number>
}

export interface AppStore extends AppState {
  setMaxWeight: (exerciseId: string, weight: number) => void
  addExercise: (exercise: Exercise) => void
  // removeExercise is NOT yet here — Plan 02-01 Task 1 adds it
}
```

From src/store/store.ts (persist merge callback — current shape):
```typescript
merge: (persisted, current) => {
  const migratedState = migrateIfNeeded(persisted as AppState)
  return { ...current, ...migratedState }
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add removeExercise to types.ts and create seedExercises.ts</name>
  <files>src/store/types.ts, src/data/seedExercises.ts</files>
  <read_first>
    - src/store/types.ts — read the full file before editing to preserve existing interfaces exactly
    - .planning/phases/02-exercise-management/02-RESEARCH.md — Exercise Seed List section for the full exercise list
  </read_first>
  <action>
**Part A — Update src/store/types.ts:**

Add `removeExercise: (id: string) => void` to the `AppStore` interface. Place it after `addExercise`. Do not change any other line.

The final AppStore interface must be:
```typescript
export interface AppStore extends AppState {
  setMaxWeight: (exerciseId: string, weight: number) => void
  addExercise: (exercise: Exercise) => void
  removeExercise: (id: string) => void
}
```

**Part B — Create src/data/seedExercises.ts:**

Create the directory `src/data/` and the file `src/data/seedExercises.ts`. The file exports a single named constant `SEED_EXERCISES` typed as `Exercise[]`.

Use exactly these exercises (50 total), sorted alphabetically within each group, all with `isCustom: false`. IDs are kebab-case of the name:

```typescript
import type { Exercise } from '../store/types'

export const SEED_EXERCISES: Exercise[] = [
  // Barbell compound
  { id: 'barbell-row',         name: 'Barbell Row',         isCustom: false },
  { id: 'bench-press',         name: 'Bench Press',         isCustom: false },
  { id: 'box-squat',           name: 'Box Squat',           isCustom: false },
  { id: 'close-grip-bench',    name: 'Close-Grip Bench Press', isCustom: false },
  { id: 'deadlift',            name: 'Deadlift',            isCustom: false },
  { id: 'deficit-deadlift',    name: 'Deficit Deadlift',    isCustom: false },
  { id: 'floor-press',         name: 'Floor Press',         isCustom: false },
  { id: 'front-squat',         name: 'Front Squat',         isCustom: false },
  { id: 'good-morning',        name: 'Good Morning',        isCustom: false },
  { id: 'hang-clean',          name: 'Hang Clean',          isCustom: false },
  { id: 'hip-thrust',          name: 'Hip Thrust',          isCustom: false },
  { id: 'incline-bench-press', name: 'Incline Bench Press', isCustom: false },
  { id: 'low-bar-squat',       name: 'Low Bar Squat',       isCustom: false },
  { id: 'overhead-press',      name: 'Overhead Press',      isCustom: false },
  { id: 'paused-squat',        name: 'Paused Squat',        isCustom: false },
  { id: 'pendlay-row',         name: 'Pendlay Row',         isCustom: false },
  { id: 'power-clean',         name: 'Power Clean',         isCustom: false },
  { id: 'push-press',          name: 'Push Press',          isCustom: false },
  { id: 'romanian-deadlift',   name: 'Romanian Deadlift',   isCustom: false },
  { id: 'squat',               name: 'Squat',               isCustom: false },
  { id: 'sumo-deadlift',       name: 'Sumo Deadlift',       isCustom: false },
  { id: 'trap-bar-deadlift',   name: 'Trap Bar Deadlift',   isCustom: false },
  { id: 'zercher-squat',       name: 'Zercher Squat',       isCustom: false },
  // Dumbbell
  { id: 'dumbbell-curl',       name: 'Dumbbell Curl',       isCustom: false },
  { id: 'dumbbell-fly',        name: 'Dumbbell Fly',        isCustom: false },
  { id: 'dumbbell-lateral-raise', name: 'Dumbbell Lateral Raise', isCustom: false },
  { id: 'dumbbell-press',      name: 'Dumbbell Press',      isCustom: false },
  { id: 'dumbbell-row',        name: 'Dumbbell Row',        isCustom: false },
  { id: 'dumbbell-shoulder-press', name: 'Dumbbell Shoulder Press', isCustom: false },
  { id: 'goblet-squat',        name: 'Goblet Squat',        isCustom: false },
  { id: 'incline-dumbbell-press', name: 'Incline Dumbbell Press', isCustom: false },
  { id: 'romanian-deadlift-db', name: 'Romanian Deadlift (DB)', isCustom: false },
  // Cable / Machine
  { id: 'cable-fly',           name: 'Cable Fly',           isCustom: false },
  { id: 'cable-row',           name: 'Cable Row',           isCustom: false },
  { id: 'face-pull',           name: 'Face Pull',           isCustom: false },
  { id: 'lat-pulldown',        name: 'Lat Pulldown',        isCustom: false },
  { id: 'leg-curl',            name: 'Leg Curl',            isCustom: false },
  { id: 'leg-extension',       name: 'Leg Extension',       isCustom: false },
  { id: 'leg-press',           name: 'Leg Press',           isCustom: false },
  { id: 'tricep-pushdown',     name: 'Tricep Pushdown',     isCustom: false },
  // Bodyweight / loaded
  { id: 'chin-up',             name: 'Chin-Up',             isCustom: false },
  { id: 'dip',                 name: 'Dip',                 isCustom: false },
  { id: 'pull-up',             name: 'Pull-Up',             isCustom: false },
  { id: 'push-up',             name: 'Push-Up',             isCustom: false },
]
```

That is 44 entries — acceptable; the requirement says "~50". Do not pad with made-up exercises. Quality over quantity.
  </action>
  <verify>
    <automated>cd "C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/lift-calc" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/seedExercises.ts` exists and exports `SEED_EXERCISES`
    - `grep -c "isCustom: false" src/data/seedExercises.ts` returns 40 or more
    - `grep "removeExercise" src/store/types.ts` returns a line with `(id: string) => void`
    - `npx tsc --noEmit` exits 0 with no errors
  </acceptance_criteria>
  <done>types.ts has removeExercise in AppStore; seedExercises.ts has SEED_EXERCISES array with 40+ entries; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Wire removeExercise action and seed loading into store.ts</name>
  <files>src/store/store.ts</files>
  <read_first>
    - src/store/store.ts — read the full file before editing; the merge callback and create factory must be edited precisely
    - src/store/types.ts — confirm AppStore now has removeExercise before adding the implementation
    - src/data/seedExercises.ts — confirm SEED_EXERCISES export name before importing
  </read_first>
  <action>
Make exactly two changes to `src/store/store.ts`:

**Change 1 — Add import at top of file:**

After the existing imports, add:
```typescript
import { SEED_EXERCISES } from '../data/seedExercises'
```

**Change 2 — Add removeExercise action inside the create() factory:**

Immediately after the `addExercise` action, add:
```typescript
      removeExercise: (id) =>
        set((state) => ({
          exercises: state.exercises.filter((ex) => ex.id !== id),
        })),
```

**Change 3 — Update the persist merge callback to seed on first launch:**

Replace the current merge callback:
```typescript
      merge: (persisted, current) => {
        const migratedState = migrateIfNeeded(persisted as AppState)
        return { ...current, ...migratedState }
      },
```

With this seeding version:
```typescript
      merge: (persisted, current) => {
        const migratedState = migrateIfNeeded(persisted as AppState)
        const merged = { ...current, ...migratedState }
        // Seed on first launch only — runs once when exercises is empty at hydration.
        // Does NOT run again when exercises becomes empty at runtime (intentional user action).
        if (!merged.exercises || merged.exercises.length === 0) {
          merged.exercises = SEED_EXERCISES
        }
        return merged
      },
```

Do NOT change any other line in the file. The onRehydrateStorage callback, CURRENT_SCHEMA_VERSION, migrateIfNeeded function, and persist config remain exactly as-is.
  </action>
  <verify>
    <automated>cd "C:/Users/MelvinKarlsson/OneDrive - Applied Value/MK-Claude-Learning-Tool/lift-calc" && npx tsc --noEmit && npm run build -- --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SEED_EXERCISES" src/store/store.ts` returns 2 matches (import + usage in merge)
    - `grep "removeExercise" src/store/store.ts` returns a match with `state.exercises.filter`
    - `grep "exercises.length === 0" src/store/store.ts` returns a match
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>store.ts has removeExercise action, SEED_EXERCISES import, and seed-on-first-launch guard in merge callback; build and type-check pass</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npx tsc --noEmit` exits 0 — no TypeScript errors across the whole project
- `npm run build` exits 0 — production build succeeds
- `grep -r "SEED_EXERCISES" src/` shows import in store.ts and export in seedExercises.ts
- `grep "removeExercise" src/store/types.ts src/store/store.ts` shows the action in both files
- `grep "exercises.length === 0" src/store/store.ts` confirms the seed guard is present
</verification>

<success_criteria>
1. `src/data/seedExercises.ts` exports SEED_EXERCISES with 40+ exercises, all `isCustom: false`, all kebab-case IDs
2. `src/store/types.ts` AppStore interface includes `removeExercise: (id: string) => void`
3. `src/store/store.ts` create() factory includes `removeExercise` that filters exercises by id
4. `src/store/store.ts` persist merge callback seeds exercises when the persisted array is empty
5. TypeScript compiles clean (`npx tsc --noEmit` exits 0)
6. Production build succeeds (`npm run build` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/02-exercise-management/02-01-SUMMARY.md`
</output>
